import {styled} from "@/stitches.config"

export const Card = styled("div", {
  backgroundColor: "$loContrast",
  borderWidth: "1px",
  borderStyle: "solid",
  borderColor: "$gray4",
  borderRadius: "$2",
})
